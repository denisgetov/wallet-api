// services/transactionService.js
// const Transaction = require('../models/Transaction');

// async function createTransaction({ userId, type, amount, status = 'success' }) {
//   const transaction = new Transaction({ user: userId, type, amount, status });
//   return transaction.save();
// }

// module.exports = { createTransaction };
